-- 打撃成績の入力ミス修正（2026-08-05 時点の調査で判明した4件）
--
-- 記録の流儀:
--   - 安打(hits) は二塁打・三塁打・本塁打を含む総安打数
--   - 得点圏打数(risp_ab) は「打数」ベース（四球・死球・犠打・犠飛は含まない）
--   - 敵失(reach_on_error) での出塁は打数(ab) に含む
-- この前提が崩れると 塁打数・打率・長打率・OPS が実際より低く出る。
--
-- 適用方法: Supabase Dashboard → SQL Editor でこのファイルの内容を実行する。
-- ※ 【1】【2】のみ実行される。【3】【4】は正しい値が不明なのでコメントアウトしてある。

begin;

-- ------------------------------------------------------------------
-- 【1】鶴岡 2026-06-07 WILDEYES戦
--   打席2 / 打数1 / 安打0 / 二塁打1 / 三振1
--   → 二塁打を打っているのに安打・打数に算入されていない。
--     正しくは「三振1 + 二塁打1」で 打数2 / 安打1。
--   影響: 通算 打率 .320→.346、塁打数 15→16、長打率 .600→.615
-- ------------------------------------------------------------------
update batting_stats
set ab = 2, hits = 1
where id = '91204537-11f8-4540-9e4a-22d94c22126b'
  and ab = 1 and hits = 0 and doubles = 1;  -- 未修正のときだけ更新（二重適用の防止）

-- ------------------------------------------------------------------
-- 【2】小澤 2026-06-07 パイオラックス戦
--   打席2 / 打数1 / 敵失1 / 打点1
--   → 敵失での出塁は打数に含むので 打数2 が正しい。
-- ------------------------------------------------------------------
update batting_stats
set ab = 2
where id = '2c34cd6c-5310-47ff-8584-0e1840913baa'
  and ab = 1 and reach_on_error = 1;

commit;


-- ==================================================================
-- ここから下は正しい値が不明なため未適用。内容を確認してから実行すること。
-- ==================================================================

-- ------------------------------------------------------------------
-- 【3】鶴岡 2026-05-23 BaySeals戦
--   打席2 / 打数1 / 安打0 / 打点1 / 得点圏打数1
--   → 打席と打数の差1が説明できない。安打なしで打点1なので犠飛の可能性が高い。
--     なお犠飛は全データで0件のため、そもそも記録していない運用かもしれない。
--   犠飛だった場合、その打席は打数に入らないので得点圏打数も見直しが必要
--   （得点圏で犠飛を打ったなら risp_ab = 0、もう一方の打数が得点圏なら risp_ab = 1 のまま）。
-- ------------------------------------------------------------------
-- update batting_stats
-- set sac_fly = 1
-- where id = 'b0b71f9c-2dda-482e-b933-ce37703de631';

-- ------------------------------------------------------------------
-- 【4】三浦貴 2026-02-28 BlueSuns戦
--   打席3 / 打数1 / 安打1 / 四球1 / 盗塁2
--   → 打数1+四球1=2 で、残り1打席の結果が不明（犠打・犠飛・敵失・打撃妨害のいずれか）。
--     スコアブックを確認して該当する項目を +1 すること。
--     ・敵失なら ab を +1（敵失は打数に含む）
--     ・犠打／犠飛なら sac_bunt / sac_fly を +1（打数には含まない）
-- ------------------------------------------------------------------
-- update batting_stats
-- set ab = 2, reach_on_error = 1
-- where id = '11561483-5a01-4e73-aee2-1e3f03e4efd0';


-- ------------------------------------------------------------------
-- 【確認用】流儀に反する行を一覧する。今後もこのクエリで健全性を点検できる。
-- ------------------------------------------------------------------
-- select g.date, g.opponent, p.name,
--        b.pa, b.ab, b.hits, b.doubles, b.triples, b.hr,
--        b.bb, b.hbp, b.sac_bunt, b.sac_fly, b.k, b.reach_on_error,
--        b.risp_ab, b.risp_hits
-- from batting_stats b
-- join games g on g.id = b.game_id
-- join players p on p.id = b.player_id
-- where b.hits < b.doubles + b.triples + b.hr   -- 安打が長打合計を下回る
--    or b.ab < b.hits                            -- 打数 < 安打
--    or b.risp_ab > b.ab                         -- 得点圏打数 > 打数
--    or b.risp_hits > b.risp_ab                  -- 得点圏安打 > 得点圏打数
--    or b.risp_hits > b.hits                     -- 得点圏安打 > 安打
--    or b.k > b.ab - b.hits                      -- 三振 > 打数−安打
--    or b.reach_on_error > b.ab - b.hits         -- 敵失 > 打数−安打
--    or b.pa <> b.ab + b.bb + b.hbp + b.sac_bunt + b.sac_fly  -- 打席の内訳が合わない
-- order by g.date;
